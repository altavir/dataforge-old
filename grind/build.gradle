apply plugin: 'groovy'

description = 'The GRIND (GRoovy INteractive Dataforge) environment'

dependencies {
    compile project(":dataforge-core")
    compile 'org.codehaus.groovy:groovy-all:2.6.+'
}