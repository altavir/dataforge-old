
description = "A ktor based http server"

dependencies {
    compile project(":dataforge-storage")
    compile "io.ktor:ktor-server-core:0.9.0"
    compile "io.ktor:ktor-server-netty:0.9.0"
}

